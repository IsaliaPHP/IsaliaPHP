<header>
    <h1><?= htmlspecialchars($post->title) ?></h1>
</header>

<main>
    <article>
        <p class="post-meta">Publicado el: <?= date('F j, Y', strtotime($post->created_at)) ?></p>
        <div class="post-content">
            <?= htmlspecialchars($post->body) ?>
        </div>
    </article>
    
    <nav>
        <?= Html::link('posts', 'Volver a publicaciones') ?>
    </nav>
</main>

